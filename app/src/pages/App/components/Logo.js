import { Box, Typography } from '@mui/material';

const Logo = () => {
  return (
    <Box sx={{ transform: 'translateY(-15%)' }}>
      <img src="/images/blast-balloon.png" alt="logo" width={264} />
    </Box>
  );
};

export default Logo;
